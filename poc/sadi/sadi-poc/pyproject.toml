[project]
name = "sadi-poc"
version = "0.1.0"
description = "SADI Q-1/Q-3 data source probe — Mars Wealthfinder"
requires-python = ">=3.10"
dependencies = [
    "feedparser>=6.0",
    "httpx>=0.27",
    "trafilatura>=1.12",
]
